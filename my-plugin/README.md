# plugins

This directory is initialized as a local Cursor plugin scaffold.

Structure:

- `.cursor-plugin/plugin.json`: plugin metadata
- `rules/`: Cursor rules
- `skills/`: Agent skills
- `agents/`: Agent definitions
- `commands/`: Custom commands
- `hooks/`: Hook configuration
- `assets/`: Static assets

Reload Cursor after linking or copying this plugin into your local plugin path.
